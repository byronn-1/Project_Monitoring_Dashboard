it("renders default view", () => {
    //  render(<App />);

    //  expect(screen.getByText('Projects Dashboard').toBeDefined);
    });

  /*
        it("should render a project name in h1", () => {
           
        });

        it("should render a project name in h1", () => {
            const firstPRender = componentToTest.findAllByType(`p`)[0];

            expect(firstPRender.children).toEqual([props.contentText]);
        })
        it("should render a project name in h1", () => {
            const secondPRender = componentToTest.findAllByType(`p`)[1]

            expect(secondPRender.children).toEqual([props.contentText]);
        })
    });
  
    it("extra-info list is visible on click", () => {

    });
    it("update project button navigates to update popup", () => {

    });
    it("see more details button navigates to project details page", () => {

    });

    describe("Order-By bar", () => {
        it("sorts list into alphabetical order", () => {
            
        });
        it("sorts list into date order", () => {
            
        });
        it("opens all list dropdowns", () => {
            
        });
        it("collapses all list dropdowns", () => {
            
        });
    }); */